<Window x:Class="TaskManagerApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Task Manager" Height="350" Width="525">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="10">
            <TextBox x:Name="taskInput" Width="300" Margin="0,0,10,0" />
            <Button Content="Add" Click="AddTask_Click" Width="65"/>
            <Button x:Name="editButton" Content="Edit" Click="EditTask_Click" Width="65"/>
            <Button Content="Remove" Click="RemoveTask_Click" Width="65"/>
            <Button x:Name="saveButton" Content="Save" Click="SaveEditedTask_Click" Width="65" Visibility="Collapsed"/>
        </StackPanel>

        <ListBox x:Name="taskListBox" Grid.Row="1" Margin="10">
            <!-- This is where tasks will be displayed -->
        </ListBox>
    </Grid>
</Window>